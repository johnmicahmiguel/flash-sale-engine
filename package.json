{
  "name": "flash-sale",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "stress:sale": "pnpm --filter @flash-sale/api stress:sale",
    "sst:dev": "sst dev",
    "sst:deploy": "sst deploy --stage=production",
    "sst:remove": "sst remove --stage=production"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.33.2",
  "devDependencies": {
    "sst": "^4.12.7",
    "turbo": "^2.3.0"
  }
}
